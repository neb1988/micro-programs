from setuptools import setup

setup(
    name='load-vulnerable-module-unreachable',
    version='0.1',
    description='A sample Python project',
    author='Your Name',
    author_email='your.email@example.com',
    packages=['load-vulnerable-module-unreachable'],
    install_requires=[
        'urllib3==1.26.4'
    ]
)