from setuptools import setup

setup(
    name='decorator-reachable',
    version='0.1',
    description='A sample Python project',
    author='Your Name',
    author_email='your.email@example.com',
    packages=['decorator-reachable'],
    install_requires=[
        'urllib3==1.26.4'
    ]
)